# ============================================================
# RetinaTag environment configuration
# Copy this file to the project root as .env:
#   cp setup/.env.example .env
# ============================================================

# --- REQUIRED: change these before running -------------------

# Secret key for signing JWT tokens. Generate one with:
#   openssl rand -hex 32
JWT_SECRET_KEY=CHANGE-ME-run-openssl-rand-hex-32

# Absolute path on the HOST machine to the directory containing
# your B-scan image files. This is mounted read-only into the
# container so the app can serve preview images.
# Example: /home/user/datasets/OCT-500/OCTA_3mm/OCT
OCT_DATA_PATH=/path/to/your/images

# --- OPTIONAL: defaults work out of the box -----------------

# Default admin account (created on first startup if no users exist)
# DEFAULT_ADMIN_USERNAME=admin
# DEFAULT_ADMIN_PASSWORD=admin

# JWT token lifetime in minutes (default: 1440 = 24 hours)
# JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Preview image format: webp | png | jpeg (default: webp)
# PREVIEW_FORMAT=webp

# Image normalization: percentile | fixed_window (default: percentile)
# NORMALIZATION_METHOD=percentile
